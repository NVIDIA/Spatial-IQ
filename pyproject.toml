[project]
name = "spatial-iq-docs"
version = "0.1.0"
description = "Documentation for the Spatial-IQ project"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "sphinx>=7.0",
    "nvidia-sphinx-theme>=0.0.9",
    "myst-parser>=2.0",
    "pillow>=10.0",
]

[tool.uv]
package = false
