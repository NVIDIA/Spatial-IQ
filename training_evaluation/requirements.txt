# Core VLMEvalKit dependencies (subset needed for local Qwen2.5-VL evaluation).
# The full VLMEvalKit requirements.txt includes API-only packages (google-genai,
# gradio, etc.) that aren't needed here and may fail to install.

# --- PyTorch (pinned to CUDA 12.4 build for cluster driver compat) ---
torch==2.5.1+cu124
torchvision==0.20.1+cu124

# --- Transformers / model loading ---
transformers
accelerate
huggingface_hub
sentencepiece
tiktoken
timm
einops
protobuf

# --- Qwen2.5-VL specific ---
qwen-vl-utils

# --- VLMEvalKit core ---
datasets
numpy
pandas
openpyxl
xlsxwriter
pillow
requests
rich
tabulate
termcolor
tqdm
typing_extensions
portalocker
sty
python-dotenv
dotenv
openai
json_repair
nest_asyncio

# --- Evaluation metrics ---
nltk
rouge
scikit-learn
scikit-image
bert_score
editdistance>=0.8.1
levenshtein>=0.20.0
sympy
pylatexenc==2.10
# latex2sympy2-extended and math-verify require Python >=3.9; skipped on 3.8.
# VLMEvalKit falls back gracefully for math answer verification.
num2words
jieba>=0.42.1
anls>=0.0.2
distance>=0.1.3
zss>=1.2.0
apted>=1.0.3
lxml>=6.0.2

# --- Image / video processing ---
opencv-python>=4.7.0.72
decord>=0.6.0
imageio
matplotlib
validators
