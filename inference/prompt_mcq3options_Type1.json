{
  "CRITICAL_INSTRUCTION_MCQ": "CRITICAL INSTRUCTION: You are undergoing a precision capability test. The input image shows a 3D structure built from objects of the same type, positioned at the center of the image. The objects are primarily boxes or cubes, but may also include stackable objects such as cylindrical objects, cans, and mugs. You will receive exactly 4 images in sequence:\n\n- Image 1 is the REFERENCE structure.\n\n- Images 2, 3, and 4 are the MULTIPLE-CHOICE OPTIONS corresponding to choices A, B, and C respectively. Each option depicts a possible coloring of the REFERENCE structure.\n\nSelect the option image that correctly answers the question by applying the described coloring to the structure. Output ONLY a single capital letter (A, B, or C) corresponding to the correct option. Do not include full sentences, explanations, pleasantries, or context.\n",
  "CRITICAL_INSTRUCTION_TASK10": "CRITICAL INSTRUCTION: You are undergoing a precision capability test. The images show a 3D structure built from objects of the same type, positioned at the center of the image. The objects are primarily boxes or cubes, but may also include stackable objects such as cylindrical objects, cans, and mugs. You will receive exactly 5 images in sequence:\n\n- Image 1 is VIEW A of an object structure.\n\n- Image 2 is VIEW B of the exact same object structure.\n\n- Images 3, 4, and 5 are the MULTIPLE-CHOICE OPTIONS corresponding to choices A, B, and C respectively. Each option depicts a possible coloring of VIEW B.\n\nSelect the option image that correctly answers the question by applying the described coloring to VIEW B. Output ONLY a single capital letter (A, B, or C) corresponding to the correct option. Do not include full sentences, explanations, pleasantries, or context.\n",
  "CRITICAL_INSTRUCTION_MAIN": "CRITICAL INSTRUCTION: You are undergoing a precision capability test. You will receive exactly 1 image of a block structure.\nYou are also given 3 MULTIPLE-CHOICE OPTIONS (A, B, C) as integer values below.\nSelect the option that correctly answers the question. Output ONLY a single capital letter (A, B, or C) corresponding to the correct option. Do not include full sentences, explanations, pleasantries, or context. Do not describe your reasoning, steps, or analysis.\n",
  "DEFINITIONS": "\nDEFINITIONS:\n1. A COLUMN is a vertical stack of one or more objects in which each object lies either on the ground or solely on top of the object immediately beneath it.\n2. A LAYER is a horizontal group of one or more objects at the same vertical height in the structure. The objects within the same layer could be laterally detached or attached. A single object also counts as a layer.\n3. A VISIBLE OBJECT is an object with at least one face fully or partially visible in the image.\n4. A CLUSTER is a group of one or more objects that are connected through direct contact, possibly across multiple layers. Two objects are considered connected if they are in direct physical contact (i.e., directly adjacent horizontally or vertically with no gap between them); diagonal placement that results in no contact does not count. This applies to both box-shaped and cylindrical objects. A cluster includes all objects that are connected directly or indirectly through such contacts.\n5. A SUPPORTING OBJECT is any object that is beneath a given object in the same column. A DIRECTLY SUPPORTING OBJECT is the supporting object in immediate contact beneath a given object. If an object is directly supported by the ground, then it has no supporting objects.\n6. A HIDDEN OBJECT is an object with no visible faces in the image. Any hidden object must be a supporting object of at least one visible object. Otherwise, such an object does not exist in a valid structure.\n",
  "task_prompts": {
    "task1": "{CRITICAL_INSTRUCTION_MCQ}QUESTION: Which option (A-C) correctly depicts the object structure(s) from Image 1 with ALL objects colored with a single color, with no extra blocks added? Your output should be ONLY a single capital letter (A, B, or C).",
    "task2": "{CRITICAL_INSTRUCTION_MCQ}{DEFINITIONS}QUESTION: Which option (A-C) correctly depicts only the top-most object(s) (i.e., the objects in the top-most layer) from Image 1 colored with a single color, with all other objects left uncolored and no extra blocks added? Your output should be ONLY a single capital letter (A, B, or C).",
    "task3": "{CRITICAL_INSTRUCTION_MCQ}{DEFINITIONS}QUESTION: Which option (A-C) correctly depicts all objects within each column of the structure in Image 1 colored with a unique color (i.e., all objects in the same column share one color, while different columns have different colors, and ALL objects are colored)? Your output should be ONLY a single capital letter (A, B, or C).",
    "task4": "{CRITICAL_INSTRUCTION_MCQ}{DEFINITIONS}QUESTION: Which option (A-C) correctly depicts all objects within each layer (i.e., all objects at the same height) of the structure in Image 1 colored with a unique color (i.e., all objects in the same layer share one color, while different layers have different colors, and ALL objects are colored)? Your output should be ONLY a single capital letter (A, B, or C).",
    "task5": "{CRITICAL_INSTRUCTION_MCQ}{DEFINITIONS}QUESTION: Which option (A-C) correctly depicts each individual object in the structure from Image 1 colored with a unique color (i.e., no two objects share the same color, and ALL objects are colored)? Your output should be ONLY a single capital letter (A, B, or C).",
    "task6": "{CRITICAL_INSTRUCTION_MCQ}{DEFINITIONS}QUESTION: Which option (A-C) correctly depicts the object(s) that are directly supporting (i.e., directly BELOW) the top-most object(s) in the structure from Image 1 colored with a single color, with all other objects left uncolored? Note: if the maximum height of all columns is 1, no objects should be colored. Your output should be ONLY a single capital letter (A, B, or C).",
    "task7": "{CRITICAL_INSTRUCTION_MCQ}{DEFINITIONS}QUESTION: Which option (A-C) correctly depicts all objects in the column(s) that contain the top-most object(s) of the structure from Image 1, excluding the top-most object(s) themselves (i.e., every object below the top-most object in those columns), colored with a single color? Note: if the maximum height of all columns is 1, no objects should be colored. Your output should be ONLY a single capital letter (A, B, or C).",
    "task8": "{CRITICAL_INSTRUCTION_MCQ}{DEFINITIONS}QUESTION: Which option (A-C) correctly depicts, for each inferred HIDDEN OBJECT in the structure from Image 1, the single VISIBLE OBJECT in immediate contact directly above it colored with a single color? No other objects higher up in the same column should be colored. Note: if there are no HIDDEN OBJECTS in the scene, no objects should be colored. Your output should be ONLY a single capital letter (A, B, or C).",
    "task9": "{CRITICAL_INSTRUCTION_MCQ}{DEFINITIONS}QUESTION: Which option (A-C) correctly depicts each distinct cluster of objects in the structure from Image 1 colored with a unique color (i.e., all objects in the same cluster share one color, while different clusters have different colors, and ALL objects are colored)? Your output should be ONLY a single capital letter (A, B, or C).",
    "task10": "{CRITICAL_INSTRUCTION_TASK10}{DEFINITIONS}QUESTION: Which option (A-C) correctly depicts all objects that are visible in VIEW A (Image 1) but are NOT visible in VIEW B (Image 2) colored with a single color, with all other objects in VIEW A left uncolored? Note: if all objects visible in VIEW A are also visible in VIEW B, no objects should be colored. Your output should be ONLY a single capital letter (A, B, or C).",
    "task11": "{CRITICAL_INSTRUCTION_TASK10}{DEFINITIONS}QUESTION: Which option (A-C) correctly depicts all objects that are visible in VIEW A (Image 1) but are NOT visible in VIEW B (Image 2) colored with a single color, with all other objects in VIEW A left uncolored? Note: if all objects visible in VIEW A are also visible in VIEW B, no objects should be colored. Your output should be ONLY a single capital letter (A, B, or C).",
    "main": "{CRITICAL_INSTRUCTION_MAIN}{DEFINITIONS}QUESTION: Which option (A-C) correctly represents the TOTAL number of objects present in the object structure of Image 1 (including any HIDDEN OBJECTS that must exist to support the visible structure)?\n\nOPTIONS:\n{choices}\n\nYour output should be ONLY a single capital letter (A, B, or C)."
  }
}
