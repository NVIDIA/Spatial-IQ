{
  "CRITICAL_INSTRUCTION": "CRITICAL INSTRUCTION: You are undergoing a precision capability test. The input image shows a 3D structure built from objects of the same type, positioned at the center of the image. The objects are primarily boxes or cubes, but may also include stackable objects such as cylindrical objects, cans, and mugs. Answer the following questions using the absolute minimum number of words possible. Output only the exact value, count, or object name. Do not include full sentences, explanations, pleasantries, or context.\n",
  "DEFINITIONS": "\nDEFINITIONS:\n1. A COLUMN is a vertical stack of one or more objects in which each object lies either on the ground or solely on top of the object immediately beneath it.\n2. A LAYER is a horizontal group of one or more objects at the same vertical height in the structure. The objects within the same layer could be laterally detached or attached. A single object also counts as a layer.\n3. A VISIBLE OBJECT is an object with at least one face fully or partially visible in the image.\n4. A CLUSTER is a group of one or more objects that are connected through direct contact, possibly across multiple layers. Two objects are considered connected if they are in direct physical contact (i.e., directly adjacent horizontally or vertically with no gap between them); diagonal placement that results in no contact does not count. This applies to both box-shaped and cylindrical objects. A cluster includes all objects that are connected directly or indirectly through such contacts.\n5. A SUPPORTING OBJECT is any object that is beneath a given object in the same column. A DIRECTLY SUPPORTING OBJECT is the supporting object in immediate contact beneath a given object. If an object is directly supported by the ground, then it has no supporting objects.\n6. A HIDDEN OBJECT is an object with no visible faces in the image. Any HIDDEN OBJECT must be a SUPPORTING OBJECT of at least one visible object. Otherwise, such an object does not exist in a valid structure.\n",
  "task_prompts": {
    "task1": "{CRITICAL_INSTRUCTION}QUESTION: What type of object(s) make up the structure at the center of the image? Give the object type name only.",
    "task2": "{CRITICAL_INSTRUCTION}{DEFINITIONS}QUESTION: How many objects are in the top-most layer (i.e., the highest layer) of the whole structure? Your answer should be a single integer ONLY.",
    "task3": "{CRITICAL_INSTRUCTION}{DEFINITIONS}QUESTION: How many columns are in the object structure? Your answer should be a single integer ONLY.",
    "task4": "{CRITICAL_INSTRUCTION}{DEFINITIONS}QUESTION: How many layers are in the object structure? Your answer should be a single integer ONLY.",
    "task5": "{CRITICAL_INSTRUCTION}{DEFINITIONS}QUESTION: How many VISIBLE objects are there in the object structure? Your answer should be a single integer ONLY.",
    "task6": "{CRITICAL_INSTRUCTION}{DEFINITIONS}QUESTION: How many VISIBLE OBJECTS are directly supporting (i.e., directly BELOW) the top-most object(s) in the whole structure? Note: the answer may be 0 if all columns have a height of 1 (i.e., no object has a supporting object). Your answer should be a single integer ONLY.",
    "task7": "{CRITICAL_INSTRUCTION}{DEFINITIONS}QUESTION: How many VISIBLE OBJECTS are in the column(s) that contain the top-most object(s), excluding the top-most object(s) themselves (i.e., every object below the top-most object in those columns)? Note: the answer may be 0 if all columns have a height of 1. Your answer should be a single integer ONLY.",
    "task8": "{CRITICAL_INSTRUCTION}{DEFINITIONS}QUESTION: First, reason about whether any HIDDEN OBJECTS must exist to support the visible structure. For each inferred HIDDEN OBJECT, identify the single VISIBLE OBJECT in immediate contact directly above it. How many such VISIBLE OBJECTS are there (i.e., VISIBLE OBJECTS that are in direct contact directly above a HIDDEN OBJECT)? Do NOT count other objects higher up in the same column. Note: the answer may be 0 if there are no HIDDEN OBJECTS in the scene. Your answer should be a single integer ONLY.",
    "task9": "{CRITICAL_INSTRUCTION}{DEFINITIONS}QUESTION: How many distinct clusters are there in the object structure? Recall that two objects are connected only if they are in direct physical contact horizontally or vertically (not diagonally). Your answer should be a single integer ONLY.",
    "task10": "{CRITICAL_INSTRUCTION}{DEFINITIONS}QUESTION: You are given two images that are two different views of the same object structure. How many objects are visible in the second view but are NOT visible in the first view? Note: the answer may be 0 if all objects visible in the second view are also visible in the first view. Your answer should be a single integer ONLY.",
    "task11": "{CRITICAL_INSTRUCTION}{DEFINITIONS}QUESTION: You are given two images that are two different views of the same object structure. How many objects are visible in the second view but are NOT visible in the first view? Note: the answer may be 0 if all objects visible in the second view are also visible in the first view. Your answer should be a single integer ONLY.",
    "main": "{CRITICAL_INSTRUCTION}{DEFINITIONS}QUESTION: How many TOTAL objects are present in the object structure? Your answer should be a single integer ONLY."
  }
}
